## What changed

## Why

## Tested

- [ ] `node --check main.js` passes
- [ ] Reloaded in a vault and exercised the affected command
- [ ] For a review rule: added or adjusted a case in the test harness, and named the official rule it mirrors
- [ ] `manifest.json` and `versions.json` untouched (bumped at release time)
